#!/bin/bash
# Retired legacy installer.
# Use 'make setup' instead. It checks system prerequisites, creates or updates
# the project virtual environment, and never runs sudo itself.
echo "This script is retired." >&2
echo "Use the project Makefile instead:" >&2
echo "  make setup" >&2
exit 0
